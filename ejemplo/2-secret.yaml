kind: Secret
apiVersion: v1
metadata:
  name: db-orders
  namespace: demo
type: Opaque
stringData:     # La conexión a la base de datos es la misma para cada tenant, 
                # definiendo el usuario y contraseña en tiempo de ejecución.
                # Al trabajar con LKE Enterprise se puede aprovechar la 
                # conectividad privada a través de la misma VPC.
  DATABASE_URL: "Host=private-abcdef.akamaidb.net;Port=10933;Database=logistics;Username=[replaced];Password=[changeme]"